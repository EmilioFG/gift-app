import { fireEvent, render, screen } from '@testing-library/react';
import { GifExpertApp } from '../../src/components';

describe('Pruebas en <GifExpertApp />', () => {

  test("probar la funcion onAddCategory()", () => {

  });

});
